#!/usr/bin/env sh

if command -v pnpm >/dev/null 2>&1; then
  pnpm lint-staged
  exit $?
fi

if command -v corepack >/dev/null 2>&1; then
  corepack pnpm lint-staged
  exit $?
fi

echo "pre-commit requires pnpm or corepack to run lint-staged." >&2
exit 127
